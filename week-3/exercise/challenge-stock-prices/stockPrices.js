sell = [15, 10, 20, 22, 1, 9]
// 12

const best = max => {

    for(let i=0; i<sell.length; i++)

    for(let j=0; j<sell.length -1; i++)

    if( [i] - [j] = (best.max) )
}


// function best(arr) {
//     let max = Math.max(...arr);
//     let maxindex = arr.indexOf(max);
//     const newValues = [];
//     for(let i = 0; i < maxindex; i++) {
//      newValues.push(max - arr[i])
//     }
//     const maxProfit = Math.max(...newValues);
//     if (maxProfit > 0) {
//      return `Max Profit $${maxProfit} - buy at $${max - maxProfit}, sell at $${max}`
//     }
//     return `0`
//    }